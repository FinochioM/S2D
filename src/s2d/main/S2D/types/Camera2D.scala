package S2D.types

import S2D.types.Vector2

case class Camera2D(
                   offset: Vector2,
                   target: Vector2,
                   rotation: Float,
                   zoom: Float,
                   )
