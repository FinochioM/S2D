package S2D

case class Texture2D(
                    id: Int,
                    width: Int,
                    height: Int,
                    mipmaps: Int,
                    format: Int,
                    )
